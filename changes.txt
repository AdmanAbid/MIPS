- added $sp
its implemented as just another register thats initialized at 255. to initialize, a line "addi $sp, $zero, 255"
is automatically added before every assembly code in the assembler.

- added some labels

- refactored assembler.py for ease of reading. functionality stays the same, except for adding $sp


problems:

- sll, srl not behaving correctly
test code:
addi $t0, $zero, 3     # 0011
sll  $t1, $t0, 1       # 0110 = 6
srl  $t2, $t0, 1       # 0001 = 1

expected:
$t1 = 6
$t2 = 1

output:
$t1 = 0
$t2 = 0

the problem might be with control rom generation. write_register selector mux is on when it should be off.